type League @model {
  id: ID!
  startDate: String!
  game: Game @connection
}

type Game @model {
  id: ID!
  name: String!
  image: String
}

type LeaguePlayer @model {
  league_id: League @connection
  user_id: String!
  user_name: String!
}
