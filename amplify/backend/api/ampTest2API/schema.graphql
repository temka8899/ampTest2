type League @model {
  id: ID!
  startDate: String!
  game: Game @connection
}

type Game @model {
  id: ID!
  name: String!
  image: String
}

type League_Player @model {
  league_id: League @connection
  user_id: String!
}
